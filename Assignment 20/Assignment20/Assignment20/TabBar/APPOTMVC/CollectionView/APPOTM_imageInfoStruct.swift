import UIKit

struct imageInfo {
    var image: UIImage
    
    static var all: [imageInfo] = [
        imageInfo(image: UIImage(named: "infoTBC1")!),
        imageInfo(image: UIImage(named: "infoTBC2")!)
    ]
}


